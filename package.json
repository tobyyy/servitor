{
  "name": "servitor",
  "version": "0.0.1",
  "description": "I am the source of the subsequent wisdom.",
  "main": "dist/app.js",
  "scripts": {
    "start": "tsc && node dist/app.js",
    "deploy-commands": "tsc && node dist/deploy-commands.js",
    "tsc": "tsc",
    "watch": "tsc -w",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Tobyyy",
  "license": "WTFPL",
  "dependencies": {
    "@discordjs/builders": "^0.6.0",
    "@discordjs/rest": "^0.1.0-canary.0",
    "discord-api-types": "^0.23.1",
    "discord.js": "^13.1.0"
  },
  "devDependencies": {
    "tslint": "^6.1.3",
    "typescript": "^4.4.3"
  }
}
